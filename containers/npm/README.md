# npm

Put npm-based metasearch apps or prototypes here when they are easier to run directly with Node than to containerize.

The shared Home Manager container module installs `nodejs`, so `npm` is available in your user environment.
